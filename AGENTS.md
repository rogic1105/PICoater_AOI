# Codex Instructions

Use `CLAUDE.md` as the project rule source.

When editing files under these areas, also read the nested rules:

- `sdk/` -> `sdk/CLAUDE.md`
- `src/dotnet/AniloxRoll.Monitor/` -> `src/dotnet/AniloxRoll.Monitor/CLAUDE.md`
- `sdk/Bridges/IoBridge/` -> `sdk/Bridges/IoBridge/CLAUDE.md`

For task-specific guidance, read the matching file under `.claude/skills/`.
Those files are Claude-oriented, but their engineering rules and workflows apply here too.

Keep `CLAUDE.md` as the single source of truth. Do not duplicate its full contents here.
